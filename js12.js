let role;

role = 'student';
role = 'cleaner';

// switch/case

switch (role) {
  case 'student': console.log('Jeroom & Co.');
    break;
  case 'instructor': console.log('Benedikt');
    break;
  default: console.log('Who is this person?');
    break;
}
/*
// if/else

if (role === 'instructor') console.log('Bene');
else if (role === 'student') console.log('names of students');
else console.log('Who is this person?')


  // ternary

  (role === 'instructor') ? (console.log('Bene')) : (role === 'student') ? (console.log('names of students')) : (console.log('Who is this person?'))


let age === 56

(age < 40) ? "young" : (age >= 40 && age < 57) ?"middle age" : (age >= 57 && salary < 77) ? "senior" : "pensionner"

  */